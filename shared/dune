; Shared is code that we use both for the modified ocamlboot interpreter and maniposynth proper.

(library
 (name shared)
 (libraries str compiler-libs.common unix)
 ; (preprocess (pps ppx_yojson_conv))
 )
